<%- include('template/header') %> 
<div>
    <% for(post of posts) { %>
        <a href="/blog/<%= post.slice(0, -3) %>">
            <div>
                <%= post.slice(0, -3).toUpperCase() %>
            </div>
        </a>
    <% } %>
</div>
<%- include('template/footer') %> 